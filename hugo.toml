baseURL = 'https://hjkunst.github.io/'
languageCode = 'en-us'
title = 'Strawberry Fields'

theme = 'archie'

[params]
  mode = 'auto'
  subtitle = 'Personal dev blog documenting Achievements, Archiving and Attempts'
  useCDN = false
  customCSS = ['css/custom.css']
  mainSections = ['posts']

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 6

# GitHub Pages 배포를 위한 설정
[deployment]
  [[deployment.targets]]
    name = "github"
    URL = "github://hjkunst/hjkunst.github.io"

# 빌드 최적화
[build]
  writeStats = false
  
[minify]
  disableCSS = false
  disableHTML = false
  disableJS = false
